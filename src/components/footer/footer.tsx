import './footer.css';

export const Footer: React.FC = () => {
    return (
        <footer className="l-footer">
            <div className="footer__inner">
                footer
            </div>
        </footer>
    );
}